/**
 * what is Array?
 * 
 * Array is a one of the most important data structure. it stores data in linear    contiguous manner in the memory. In Array, how we store data in most important than what kind of data we are storing.
 * 
 * Example: we need to store information of a picture so we will choose array because photo needs to store data in a continuous manner.
 * 
 * for deleting key-value pair: delete x.name
 */